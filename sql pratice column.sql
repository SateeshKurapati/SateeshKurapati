use college
select * from student
select max(marks), min(marks), sum(marks), avg(marks), count(marks) from student
select max(marks) as maximum, min(marks) as minimum, sum(marks), avg(marks), count(marks) from student
